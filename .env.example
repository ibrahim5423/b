# Server-side — never expose this to the client
ANTHROPIC_API_KEY=your_anthropic_api_key_here
PORT=3001

# Client-side — safe to expose via VITE_ prefix
VITE_VAPI_PUBLIC_KEY=your_vapi_public_key_here
